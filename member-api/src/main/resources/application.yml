spring:
  application:
    name: member
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8071
  config:
    import: optional:configserver:http://localhost:8071

management:
  endpoints:
    web:
      exposure:
        include: ['refresh','beans','env']